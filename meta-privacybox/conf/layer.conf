# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "privacybox"
BBFILE_PATTERN_privacybox = "^${LAYERDIR}/"
BBFILE_PRIORITY_privacybox = "6"

LAYERDEPENDS_privacybox = "core"
LAYERSERIES_COMPAT_privacybox = "scarthgap"


